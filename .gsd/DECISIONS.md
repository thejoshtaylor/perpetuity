# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001-6cqls8/S01 | arch | Session token storage mechanism | httpOnly cookie sessions | XSS-safe; browser sends Cookie header on WebSocket upgrade requests transparently, enabling the same auth mechanism for REST and WS without extra token-passing machinery. localStorage JWT is replaced entirely. | No — WebSocket auth depends on this; changing would require reworking WS auth in M002 | collaborative |
| D002 | M001-6cqls8/S01 | arch | Role model structure for user and team permissions | UserRole enum on User + TeamRole enum on TeamMember join table | A user can be team admin on Team A and member on Team B — the role must live on the membership, not the user. System admin is user-level (covers all teams). Replaces is_superuser bool. | No — downstream role checks throughout M002–M005 depend on this shape | collaborative |
| D003 | M001-6cqls8/S02 | arch | Personal team identification and invite restriction | is_personal boolean flag on Team model; invite endpoints reject personal teams at API layer | Simplest way to distinguish personal teams without a separate model. API enforcement prevents accidental sharing via invite. | Yes — if personal teams ever gain special sharing semantics in a future milestone | collaborative |
| D004 | M001-6cqls8 discussion — applies from M002 onward | arch | Terminal workspace isolation model | Per-user-per-team Docker containers with dedicated mounted volumes at /workspaces/user_id/team_id/ | User on 3 teams gets 3 independent container environments. Projects are team-scoped; when a user works on a team project, it copies into that team's container. Claude Code/Codex credentials are also per-team, configured inside each container. | No — this is the core product isolation promise | collaborative |
| D005 | M001-6cqls8 discussion — applies from M002 onward | arch | Container orchestration approach | Dedicated orchestrator service in Docker Compose wrapping docker-py/aiodocker; Celery workers request containers from it via HTTP | Clean separation between container lifecycle management and application logic. Celery workers don't need Docker socket access directly. Orchestrator owns container state in Postgres (not Redis — not hot-path enough). | Yes — if scale requires moving to Kubernetes or a dedicated scheduler | collaborative |
| D006 | M001-6cqls8 discussion — applies from M002 onward | arch | Multiple terminal instances per user-team | Multiple container processes sharing a single mounted volume per user-team | User can open multiple terminal windows; each is a separate pty process / container instance but all operate on the same /workspaces/user_id/team_id/ filesystem. Provides familiar multi-pane terminal UX without filesystem divergence. | Yes — if resource constraints require a different multiplexing approach (e.g. tmux inside a single container) | collaborative |
| D007 | M001-6cqls8 discussion — applies from M004 onward | arch | AI CLI integration approach (Claude Code + Codex) | TTY workaround via `script -q /dev/null claude -p '...'` inside user containers; `--dangerously-skip-permissions` for automated use; per-team API keys stored encrypted in Postgres | Claude Code CLI has a TTY dependency that prevents direct subprocess invocation from Celery. TTY workaround is pragmatic and works today. Per-team keys provide isolation — user A's Claude credentials on Team B don't leak to Team A. | Yes — if Anthropic SDK provides a stable programmatic interface that eliminates the TTY requirement | collaborative |
| D008 | M001-6cqls8 discussion | arch | Frontend state management split | React Query for server-fetched data; Zustand for live connection state (active terminals, notification queue) | React Query handles cache invalidation and server sync well; Zustand handles the ephemeral real-time state that doesn't belong in the server cache (open WS connections, active terminal sessions, notification queue). | Yes — if the state complexity grows enough to warrant a different approach | collaborative |
| D009 | M001-6cqls8 discussion — applies from M005 onward | arch | Workflow execution engine | Celery with Postgres as state store; container acquired on demand only when a step requires terminal space | Celery provides retry, scheduling, and task isolation. Acquiring containers only when needed avoids always-on cost. Run history + per-step records stored in Postgres for full failure visibility. | Yes — if task volume or latency requirements push toward a different queue | collaborative |
| D010 | M001-6cqls8 discussion — applies from M003 onward | arch | GitHub connections scope | Multiple GitHub connections per team (org-level or personal OAuth/PAT); personal teams use personal GitHub connections; system admin can set system-level defaults | Teams use different GitHub orgs; multiple repos from different orgs need independent auth. Per-team scoping matches the workspace isolation model. | Yes — if a unified GitHub App installation model is preferred later | collaborative |
| D011 | M001-6cqls8/S01 | arch | How to create the TeamMember join table in S01 when the Team model is owned by S02 | S01 creates a minimal Team stub (id UUID PK + created_at only) alongside TeamMember, so TeamMember's team_id FK resolves. S02 extends Team with real columns (name, slug, is_personal) via a new migration. | The M001-6cqls8 roadmap boundary map explicitly says S01 produces "TeamMember join table schema with TeamRole enum" AND "Alembic migration for role enum + TeamMember table". TeamMember needs a FK target. Creating a Team stub here (vs. moving TeamMember to S02) preserves the boundary map, keeps S01 self-contained for its WS integration test, and lets S02 add columns without schema surgery on TeamMember. Alternative (TeamMember without FK) would defer referential integrity and make S02 messier.</rationale>
<parameter name="revisable">No — the stub + extend-in-S02 pattern is locked in by the S01/S02 boundary | Yes | agent |
| D012 | M002-jy6pde planning | arch | Pty durability strategy for terminal sessions | Each session_id is a named tmux session inside the (user, team) container; orchestrator restart kills the docker exec stream but tmux keeps the shell and scrollback alive. Reattach via a fresh exec into the existing tmux session. | Direct docker exec ptys die when the orchestrator restarts because the exec stream is owned by the orchestrator process. Tmux as the pty owner decouples shell lifetime from orchestrator lifetime. Refines D006 (multiple panes per user-team): instead of multiple containers per user-team sharing a volume, M002 ships ONE container per (user, team) with multiple named tmux sessions inside it — same multi-pane UX, lower resource cost, and durability is provided by tmux rather than container restart policy. Alternatives considered: plain docker exec (rejected — cannot survive orchestrator restart); screen (rejected — tmux has better capture-pane API and is more commonly preinstalled); custom pty supervisor process (rejected — reinventing tmux). | No — every M002 slice and every M002+ milestone that uses the terminal depends on this shape | collaborative |
| D013 | M002-jy6pde planning | arch | Live session registry storage for the orchestrator | Add redis:7-alpine as a new compose service on the internal network, password-authed via shared secret. Orchestrator stores session_id → {container_id, tmux_session, user_id, team_id, last_activity} in Redis. Redis unreachable → 503 (no in-memory fallback). | D005 said container state lives in Postgres, not Redis. M002 refines that for the per-WS-frame heartbeat hot path: container LIFECYCLE records (workspace_volume table, container_id history) stay in Postgres; per-session ACTIVITY (last_activity, live attach map) goes to Redis. Updating Postgres on every WS frame would amplify writes for no durability gain. No in-memory fallback because falling back would lie about persistence — a session marked alive in memory but not in Redis would silently drop on orchestrator restart. Alternatives: in-process dict (rejected — lost on orchestrator restart, defeats tmux durability); Postgres heartbeat row (rejected — write amplification); SQLite on shared volume (rejected — locking pain across orchestrator workers). | Yes — could move to Postgres if write amplification is shown to be tolerable, or to a different cache if Redis ops cost becomes an issue | collaborative |
| D014 | M002-jy6pde planning | arch | Per-workspace volume storage with hard size cap | Per-workspace sparse .img files at /var/lib/perpetuity/vols/<volume_id>.img, mounted via losetup then bind-mounted into the container at /workspaces/<user_id>/<team_id>/. Cap is enforced by the ext4 filesystem size — kernel-level, not advisory. New workspace_volume Postgres table (id, user_id, team_id, size_gb, img_path, created_at) holds the per-volume effective cap. Orchestrator runs with CAP_SYS_ADMIN (or root) on the host for losetup/mount/resize2fs. | Bind-mounted directory volumes can't enforce a size limit. ZFS/btrfs quotas require host filesystem buy-in we don't want to mandate. Loopback ext4 works on any Linux host. Per-volume size_gb (rather than deriving from the global setting) is the single source of truth for the effective cap, which is what enables the partial-apply shrink rule in D015. Alternatives: plain bind mount (rejected — no size cap); Docker named volume with size limit (rejected — not portable across drivers); ZFS dataset per workspace (rejected — host-filesystem dependency). | Yes — could swap to a different size-capped storage backend (ZFS, btrfs, Docker driver plugin) if the CAP_SYS_ADMIN deployment constraint becomes painful | collaborative |
| D015 | M002-jy6pde planning | arch | Volume cap configurability and partial-apply shrink rule | system_settings table in Postgres (key TEXT PK, value JSONB) with admin API GET/PUT /api/v1/admin/settings[/{key}] gated by role==system_admin. workspace_volume_size_gb is the canonical key for the default new-volume cap. Partial-apply shrink: PUT to a smaller value than at least one existing volume's size_gb returns 200 with a warnings payload listing affected (user_id, team_id, current size_gb, current usage_bytes); existing rows keep their old size_gb (cap divergence allowed); new rows use the new global default; rows with size_gb <= N are eligible to grow on next provision via resize2fs+losetup -c. Force-shrink with grace period is deferred — schema is ready (workspace_volume.size_gb is per-row), a future ops milestone adds grace_period_expires_at + reaper without migration. | Generic key/value PUT-per-key chosen over a typed Pydantic doc — loses OpenAPI type clarity and shifts validation to per-key runtime validators, but lets us add settings without touching the schema and avoids whole-doc-overwrite footguns. Per-volume size_gb (not derived from the global setting) is the single source of truth for the effective cap, which makes partial-apply trivial. Always-refuse shrink rejected — admins need a path to lower the global default for new workspaces even when some old ones are oversize. Force-shrink with `force: true` flag rejected — user prefers the grace-period model. Per-key validators are not optional: they catch typo-shape errors (`"2"` vs `2`) at the PUT boundary instead of silently storing wrong types. | Yes — could add force-shrink-with-grace-period in a future ops milestone; could migrate to a typed Pydantic settings doc later if OpenAPI clarity becomes more valuable than schema flexibility | collaborative |
| D016 | M002-jy6pde planning | arch | Backend ↔ orchestrator authentication and key rotation | Shared secret env vars ORCHESTRATOR_API_KEY (current) and ORCHESTRATOR_API_KEY_PREVIOUS (optional). Orchestrator accepts either on every HTTP and WS request. Backend always sends ORCHESTRATOR_API_KEY. WS bridge architecture: browser ↔ FastAPI WS (cookie-authed) ↔ orchestrator WS (shared-secret-authed) ↔ tmux exec stream. FastAPI proxies bytes; orchestrator owns Docker. | Two-key acceptance lets ops roll the secret without simultaneous restart of both services — set previous=old, current=new on orchestrator first, then update backend. Modest implementation cost (one extra config value, one extra acceptance branch); meaningful operational value. Two-hop bridge keeps the Docker socket out of the backend (D005). Direct browser → orchestrator WS rejected — would require exposing orchestrator publicly and reimplementing cookie auth there. Single static key rejected — user wanted graceful rotation in scope. mTLS rejected — certificate plumbing overkill for an internal-network service-to-service call in M002. Hot rotation beyond the two-key acceptance pattern (SIGHUP, admin endpoint) is out of scope — env-var based only. | Yes — could move to mTLS or a token issuer if the internal-network assumption breaks (e.g. orchestrator moves out-of-cluster) | collaborative |
| D017 | M002-jy6pde planning, S01 | arch | WS frame protocol between browser, backend, and orchestrator | JSON-framed protocol on both legs of the WS bridge. Server frames: {type:"attach",scrollback}, {type:"data",bytes}, {type:"exit",code}, {type:"detach",reason}. Client frames: {type:"input",bytes}, {type:"resize",cols,rows}. Scrollback hard-capped at 100 KB on the orchestrator side (not trusting tmux to limit). Resize uses default tmux semantics: smaller of attached clients wins. Frame shape locked at end of S01 — downstream slices must not change shape. | JSON framing accommodates both control messages (resize, attach) and data without an out-of-band channel. Raw byte stream rejected — no good way to multiplex resize/attach/exit signals. Hard-cap on orchestrator side (rather than relying on tmux) prevents oversized attach frames from breaking the WS bridge (WS message size limits) — tmux capture-pane discipline is an orchestrator responsibility, not a downstream caller's. Locking the protocol at S01 end gives S02–S05 (and the eventual frontend in a later milestone) a stable contract to build on. | Yes — additive frame types are non-breaking and fine to add later; existing frame shapes are locked for the lifetime of M002+ until a versioning bump | collaborative |
| D018 | M002-jy6pde planning | arch | Container quota model and idle reaper strategy | One container per (user, team) — name perpetuity-ws-<first8-team>, Docker labels user_id/team_id for discovery — with unlimited tmux sessions inside. Resource limits per container: mem_limit=2g, pids_limit=512, nano_cpus configured per env. Idle reaper: orchestrator background task runs every N seconds; for each session, if Redis last_activity exceeds idle_timeout_seconds AND in-memory active-WS map has no live attach, kill the tmux session and reap the container (volume persists). Default idle timeout 15 min, configurable via system_settings.idle_timeout_seconds. | Single container per (user, team) with multiple tmux sessions inside (D012) — preserves D004 isolation while delivering R008's multi-pane UX without container-per-tab cost. Refines D006: the "multiple processes per user-team" model is now "one container, multiple tmux sessions". Two-phase idle check prevents the race where a freshly-attached client hasn't yet updated last_activity. 15 min default is aggressive enough to control idle resource use, softened by tmux+scrollback replay so reattach feels seamless. Hard caps prevent one runaway workspace from starving the host. | Yes — quota numbers (mem_limit, pids_limit, idle timeout) are tunable; the one-container-per-(user,team) shape is locked until a future scaling milestone | collaborative |
| D019 | M004-guylpp planning, Layer 2 | arch | GitHub authentication mechanism for per-team connections | Single GitHub App we own (not OAuth App, not Anthropic-affiliated, not personal access tokens). Each team connection = one App installation against an org or personal account. Multiple installations per team allowed. | GitHub App matches team-level connection semantics natively (installation = connection); short-lived 1h installation tokens limit encryption-at-rest blast radius; first-class per-installation webhook delivery; owning the App ourselves means we control rotation cadence and the webhook secret without depending on Anthropic identity. OAuth App rejected — user-scoped tokens collide with team-level connections, deprecated path. PAT rejected — no per-installation webhook delivery, no token short-life property, worse UX. Anthropic-affiliated app rejected — explicit user requirement that the app be ours. | No — every M004 GitHub-touching surface depends on this shape; switching would require migration of installation state and rewriting auth flows | collaborative |
| D020 | M004-guylpp planning, Layer 2 | arch | Storage mechanism for sensitive system_settings values (GitHub App private key, webhook secret, future SMTP password) | Extend the existing M002 system_settings table with three columns: value_encrypted BYTEA NULL, sensitive BOOLEAN NOT NULL DEFAULT FALSE, has_value BOOLEAN NOT NULL DEFAULT FALSE. Existing value JSONB column stays for non-sensitive keys (back-compat). The _VALIDATORS registry extends to {validator, sensitive: bool, generator: Callable \| None}. Encryption: Fernet from cryptography library, key from env SYSTEM_SETTINGS_ENCRYPTION_KEY (32 url-safe base64 bytes), fail-fast at app boot if missing while any sensitive key is registered. Decrypt only at the call site that needs the plaintext — never in the API layer that returns to UI. | M002's registered-key + per-key validator pattern already does most of the wiring; a parallel system_settings_secret table would double the work (two admin endpoints, two registries, two migration paths) and split the operator UI. One admin surface keeps the operator mental model simple. Fernet is library-vetted AEAD, no manual nonce management. Env-keyed encryption keeps the trust boundary at the env (same boundary as Postgres password). Decrypting at call site only ensures plaintext never round-trips through the API layer that could log or echo it. | Yes — could move to KMS or external secrets manager later if multi-tenancy or compliance demands; the encrypt_setting / decrypt_setting interface stays stable across backends | collaborative |
| D021 | M004-guylpp planning, Layer 2 | arch | Installation token caching strategy | Mint installation tokens on demand (JWT-from-private-key → POST /app/installations/{id}/access_tokens), cache in Redis keyed by installation_id with 50-minute TTL (10-min safety margin under GitHub's 1h TTL), never persist to Postgres. Lazy population: cache miss → mint → store. Cache-miss race accepted: two concurrent ops both mint, both write last-write-wins. | ~150ms per token mint stacks fast across clone/push/webhook hot paths. Redis is already wired (orchestrator/app/redis_client.py). Short-lived tokens limit encryption-at-rest blast radius — they expire faster than they would survive a leak, so we don't have to encrypt them. Postgres token cache rejected — Redis is the right tool for short-TTL caches; Postgres would replicate every token write. Redis lock to serialize mints rejected — single redundant mint is cheaper than the lock complexity. | Yes — TTL window is tunable; cache backend could move if Redis becomes a bottleneck | collaborative |
| D022 | M004-guylpp planning, Layer 2 | arch | Team-mirror container topology — image, role, lifecycle | One team-mirror container per team. Same workspace-image as user containers (full toolchain — git, claude CLI, codex CLI). Per-team Docker volume holds bare repos at /repos/<project_id>.git. Lifecycle reuses M002's volume_store.py / sessions.py / reaper.py shapes. Spin-up triggers: any user git fetch, any webhook, any open-project call, any explicit ensure-mirror admin call. Per-team mirror_idle_timeout_seconds reaper (separate from user-session reaper) with system_settings fallback. Team admin can disable auto-reap → always-on for that team. Dual role: git mirror in M004; workflow execution host in M005. | The mirror's second job — landing in M005 — is to be the team's workflow execution host. Workflows need the full toolchain, so a slimmer dedicated mirror image was a false economy. One image to maintain, one container class with parametrized labels (perpetuity.team_mirror=true vs the user-workspace label). Same lifecycle shape means reusing battle-tested M002 code, not inventing a new lifecycle. One container per team (not per project) avoids container-per-project lifecycle explosion; project granularity inside one container is just file paths. Mirror reaper is separate from user-session reaper because the failure modes differ — reaping mid-fetch breaks user clone, reaping mid-push breaks auto-push. | Yes — could split mirror image and workflow image later if the workflow host needs different security boundaries | collaborative |
| D023 | M004-guylpp planning, Layer 2 | arch | Mirror→user transport protocol | Inside the team-mirror container: git daemon --base-path=/repos --export-all --reuseaddr --enable=receive-pack on port 9418. User containers clone/push by git://team-mirror-<team_id>:9418/<project_id>.git over the compose network. No credentials in user-side .git/config. Trust boundary is the compose network itself. | Honest two-hop boundary — git push from user to mirror is a real git op, not a bind-mount FS write. Future per-user auth can be added by swapping git daemon for git http-backend without rewriting the user-side. Shared bind-mount of /repos rejected — FS-lock failure mode where a user container holding the lock blocks the mirror; concurrent writers are unsafe; breaks the two-hop abstraction. git http-backend over HTTPS in M004 rejected — no per-user auth needed yet, defer until use case appears. | Yes — swap to git http-backend when per-user auth becomes a requirement | collaborative |
| D024 | M004-guylpp planning, Layer 2 | arch | GitHub credential discipline in two-hop clone | GitHub→mirror clone uses installation token injected via env vars on the orchestrator's docker exec call into the mirror container — never written to .git/config. The cloned remote URL is sanitized to bare https://github.com/<owner>/<repo>.git after clone; subsequent ops re-inject the token on each call. Mirror→user clone is over git daemon with zero credentials. Push-back follows the same two hops in reverse: user push → mirror; if rule=auto, mirror push to GitHub origin with a fresh installation token. Auto-push rule schema (project_push_rules table) ships with all three modes (auto, rule, manual_workflow); only the auto executor is wired in M004 — rule and manual_workflow modes are stored but inert until M005 lights up the workflow engine. | Credentials never touch the user container. Atomic from the user's POV — GitHub→mirror failure aborts before user-side clone starts. Schema-now / executors-deferred for push rules is cheap; deferring storage to M005 would force a migration on top of M005's workflow tables, and storing all three modes now means the UI lands once with M005 only adding executors — clean phase boundary. | No — env-on-exec credential discipline is a security invariant; rule schema shape is locked in by S04 plan | collaborative |
| D025 | M004-guylpp planning, Layers 2 and 3 | arch | Webhook receiver semantics and decrypt-failure policy | POST /api/v1/github/webhooks reads X-Hub-Signature-256, computes hmac.compare_digest against the current github_app_webhook_secret plaintext (decrypted at the call site only). On HMAC pass: persist event row (UNIQUE on delivery_id for idempotency under GitHub's 24h retry), call dispatch_github_event(event_type, payload) (no-op stub in M004; M005 fills the body), 200. On HMAC fail: insert webhook_rejections audit row (delivery_id, signature_present, signature_valid, source_ip), 401, no event-body persistence. Generate-and-display-once flow for the webhook secret: POST /admin/settings/{key}/generate runs secrets.token_urlsafe(32) backend-side, encrypts and stores, returns the value once in the response; subsequent reads return has_value:true with no value. Re-generate is intentionally destructive (old webhooks 401 until upstream is updated). Fernet decrypt failure (cryptography.fernet.InvalidToken) at any decrypt call site emits structured ERROR log with key name and surfaces as 503 {detail: 'system_settings_decrypt_failed', key: '<name>'} — never silently fall back. | Webhook hygiene — bad signatures must not consume DB row space (DoS protection), good signatures must persist for replay debugging. Dispatch hook landing as a real (no-op) function in M004 makes M005's wiring one-line and the failure path testable now. delivery_id UNIQUE constraint enforces idempotency at the storage layer, matching GitHub's 24h retry semantics. Backend-seeded generate keeps the secret seed inside the trust boundary; the one-time-display window is the only moment plaintext crosses backend→UI, and only for the second the admin needs it. Fail-loud-on-decrypt is required because silent fallback after key rotation would mean webhooks pass HMAC against a stale secret while the admin thinks the new one is in effect — a security-state-vs-operator-intent divergence bug. | No — HMAC verification, idempotency, and fail-loud-on-decrypt are security invariants | collaborative |
| D026 | M005-oaptsz/S04 planning | frontend/voice-input | Use primitive-level auto-wrapping for universal voice coverage | Wrap eligible `Input` and new `Textarea` primitives with voice controls by default, with explicit opt-out props/data attributes for password, OTP, secrets, and sensitive/debounced fields. | S04 requires microphone coverage next to every text input; enforcing at shared primitives prevents missed consumers while keeping sensitive fields safe through a small, auditable opt-out list. This extends S01's design-system-primitive-floor pattern for mobile touch targets. | Yes | agent |
